{
  "name": "swarm-monorepo",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "npm test --workspace=@swarm/agent && npm test --workspace=@swarm/api"
  },
  "devDependencies": {
    "turbo": "2.5.3"
  }
}
