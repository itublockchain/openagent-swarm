# Shell scripts and Dockerfiles must stay LF: Windows checkouts under
# core.autocrlf=true otherwise smuggle \r into shebangs and heredocs,
# producing "exec /app/start.sh: no such file or directory" at runtime.
*.sh         text eol=lf
Dockerfile   text eol=lf
